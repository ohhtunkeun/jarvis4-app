import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './자비스앱';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
